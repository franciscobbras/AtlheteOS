// Tab "Life" — vazia por agora (placeholder). O conteúdo antigo (mock + dados do
// wearable) saiu: os dados do wearable estão agora em /wearable-data.
export default function LifePage() {
  return (
    <div className="animate-fade-in" style={{ display: 'grid', gap: 16 }}>
      <div className="page-header">
        <h1 className="page-title">Life</h1>
      </div>
      <p style={{ color: 'var(--muted)', fontSize: 14 }}>Em breve.</p>
    </div>
  );
}
